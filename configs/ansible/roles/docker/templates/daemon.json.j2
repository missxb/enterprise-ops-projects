{
  "storage-driver": "overlay2",
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "registry-mirrors": [
    {# [已停用] 以下镜像站已停止服务，仅保留作为备用参考 #}
    {# "https://docker.m.daocloud.io" #}
  ]
}
